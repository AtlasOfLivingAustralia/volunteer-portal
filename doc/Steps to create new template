Steps to create new template
============================

1. Create a csv file under Configuration -> resources with the name of the template (i.e. aerialObservations.csv). Include in this file the field defintions for this template.

2. Make sure template is created if not already existing in the bootstap.groovy section - see existing template checks to see how this is done

        template = Template.findByName("AerialObservations")
        if (!template) {
            println "creating new Template: AerialObservations"
            template = new Template(name: "AerialObservations", viewName: "aerialObservationsTranscribe", author: 'webmaster@ala.org.au', created: new Date(), fieldOrder: '', viewParams: [:]).save(flush: true, failOnError: true)
        }
        populateTemplateFields(template, "aerialObservationsFields")

3. Create the transcribe view GSP (use the name you specified as viewName in step 2)

4. Implement an import function for your template (if required) on the TaskLoadService. Import functions take the form 'import_<templateName>'. For example

    def import_AerialObservations = { TaskDescriptor taskDesc, String[] tokens ->
		....        
    }

5. Implement an export function for your template (if required) on the ExportService. Export functions take the form 'export_<templateName'. For example

	def export_AerialObservations = { Project project, taskList, valueMap, List fieldNames, response ->
		...
	}





